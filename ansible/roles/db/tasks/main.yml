- name: Install PostgreSQL and deps
  apt:
    update_cache: true
    name:
      - postgresql-16
      - postgresql-client-16
      - postgresql-contrib
      - python3-psycopg2
    state: present
  tags: [db]

- name: Ensure PostgreSQL cluster exists
  become: true
  shell: |
    set -e
    # Detect any cluster for the current version
    if pg_lsclusters | grep -q "{{ postgresql_version }}"; then
      echo "Cluster already exists, skipping creation."
    else
      echo "No cluster found, creating..."
      pg_createcluster {{ postgresql_version }} main --start
    fi
  args:
    executable: /bin/bash
  register: pg_cluster_check
  changed_when: "'creating' in pg_cluster_check.stdout"
  tags: [db]


- name: Ensure PostgreSQL is running
  service:
    name: postgresql
    state: started
    enabled: true
  tags: [db]

- name: Template postgresql.conf
  template:
    src: postgresql.conf.j2
    dest: "/etc/postgresql/{{ postgresql_version }}/main/postgresql.conf"
    owner: postgres
    group: postgres
    mode: "0644"
  notify: Restart PostgreSQL
  tags: [db]

- name: Template pg_hba.conf
  template:
    src: pg_hba.conf.j2
    dest: "/etc/postgresql/{{ postgresql_version }}/main/pg_hba.conf"
    owner: postgres
    group: postgres
    mode: "0640"
  notify: Restart PostgreSQL
  tags: [db]

- name: Create database if not exists
  become: true
  become_user: postgres
  shell: |
    psql -tc "SELECT 1 FROM pg_database WHERE datname='{{ db_name }}'" | grep -q 1 || \
    psql -c "CREATE DATABASE {{ db_name }};"
  args:
    executable: /bin/bash
  tags: [db]

- name: Create DB user if not exists
  become: true
  become_user: postgres
  shell: |
    psql -tc "SELECT 1 FROM pg_roles WHERE rolname='{{ db_user }}'" | grep -q 1 || \
    psql -c "CREATE USER {{ db_user }} WITH PASSWORD '{{ db_password }}';"
  args:
    executable: /bin/bash
  tags: [db]

- name: Grant schema privileges
  become: true
  become_user: postgres
  shell: |
    psql -d {{ db_name }} -c "GRANT ALL PRIVILEGES ON SCHEMA public TO {{ db_user }};"
  args:
    executable: /bin/bash
  tags: [db]
