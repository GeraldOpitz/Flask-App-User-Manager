# Allow local connections as postgres without password
local   all             postgres                                trust

# Local loopback
host    all           all           127.0.0.1/32          md5

# Allow remote connections
host    {{ db_name }} {{ db_user }} 0.0.0.0/0             md5
